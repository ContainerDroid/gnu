#!/bin/bash

adb kill-server
adb root
adb wait-for-device
adb push bin/rsyncd.conf /sdcard/
adb shell killall rsync
adb shell rsync --daemon --no-detach --config=/sdcard/rsyncd.conf --log-file=/proc/self/fd/2 &
adb forward tcp:6011 tcp:1874
